class Solution:
   def finalvalueafteroperations(self, operations):
       x=0

       for operation in operations:
           if "+" in operations:
               x +=1
           else:
                x -= 1

      return x
