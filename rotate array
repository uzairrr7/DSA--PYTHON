class solution:
  def rotate(self,nums,k):
      n = len(nums)
      nums.reverse()

      nums[:k] = reversed(nums[:k])
      nums[k:] = reversed(nums[k:])

solution = solution()
nums = [1,2,3,4,5,6,7]
k = 3
solution.rotate(nums ,k)
print("array:" , nums)
